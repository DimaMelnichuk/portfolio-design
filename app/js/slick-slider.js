$(function(){

	/***************** Slick-slider for post ********************/
  $('.page-slider').slick({
	  arrows: false,
	  dots: true,

  });

  $('.gallery-post__slider').slick({

  });

  $('.testi-slider').slick({
		arrows: false,
		autoplay: true,
		autoplaySpeed: 5000,
		waitForAnimate: false,
  });

});