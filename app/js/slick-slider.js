$(function(){

	/***************** Slick-slider for post ********************/
  $('.post-slider').slick({
	  arrows: false,
	  dots: true,
	  waitForAnimate: false,
  });

  $('.gallery-post__slider').slick({

  });

  $('.testi-slider').slick({
		arrows: false,
		autoplay: true,
		autoplaySpeed: 5000,
		waitForAnimate: false,
  });



  
});